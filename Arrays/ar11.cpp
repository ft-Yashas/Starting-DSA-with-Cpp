#include<iostream>
using namespace std;


int main()
{
    int nums[]={2,7,11,15};
    int target=9;
    int size = sizeof(nums)/sizeof(nums[0]);
    while(size>0)
    {
        if()
    }
    return 0;
}